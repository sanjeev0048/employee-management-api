# Employee Management API (FastAPI + SQLite)

A simple backend REST API built with **FastAPI** and **SQLAlchemy**, using **SQLite** as the database.

## 🚀 Features
- Create, Read, Update, and Delete employees
- SQLite database — no setup required
- Interactive Swagger UI (`/docs`)

## 🧰 Tech Stack
- FastAPI
- SQLAlchemy
- Pydantic v2
- SQLite

## ▶️ Run Locally
```bash
pip install -r requirements.txt
uvicorn main:app --reload
